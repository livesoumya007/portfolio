import styles from './Contact.module.css';

export default function Contact() {
  return (
    <footer id="contact" className={styles.contact}>
      {/* Add content here — wrap each block in <FadeUp> for scroll entry animation */}
    </footer>
  );
}

